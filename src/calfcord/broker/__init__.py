"""The local Tansu broker launcher (the substrate's message bus).

The broker binary is provided by the ``calfkit-mesh`` dependency; this package
only wraps it in the ``calfcord-broker`` console script (see :mod:`.runner`).
"""
